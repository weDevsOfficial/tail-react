name: Publish to NPM

on:
  release:
    types: [created]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish package on NPM
        run: |
          echo "=== NPM Debug Info ==="
          npm whoami --registry https://registry.npmjs.org 2>&1 || true
          echo "=== Testing publish dry-run ==="
          npm publish --access public --provenance --dry-run --registry https://registry.npmjs.org 2>&1 || true
          echo "=== Actual publish ==="
          npm publish --access public --provenance